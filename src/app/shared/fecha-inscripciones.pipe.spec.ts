import { FechaInscripcionesPipe } from './fecha-inscripciones.pipe';

describe('FechaInscripcionesPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaInscripcionesPipe();
    expect(pipe).toBeTruthy();
  });
});
